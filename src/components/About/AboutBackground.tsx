import React from "react";

// Ambient blobs retired for the clean/clear-glass look.
const AboutBackground = React.memo(() => null);

AboutBackground.displayName = "AboutBackground";
export default AboutBackground;
